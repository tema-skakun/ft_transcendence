import { User } from "src/entities/user/user.entity";
import { MatchHistoryEntry } from "./matchHistoryEntry/matchHistoryEntry.entity";

const entities = [User, MatchHistoryEntry];

export {User};
export default entities;